export enum RequestMethod {
    get = 'get',
    post = 'post',
    put = 'put',
    delete = 'delete'
}

export enum Role {
    intern = 'intern',
    mentor = 'mentor'
}

export enum CacheMode {
    file = 'file',
    runtime = 'runtime',
    redis = 'redis'
}