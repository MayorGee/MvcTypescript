import AbstractView from '../AbstractView.js';

export default class InternInsertView extends AbstractView {
    constructor() {
        super();
        
        this.template = './intern/intern-insert';
    }
}